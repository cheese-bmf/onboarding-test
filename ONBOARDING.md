# b.stage 커스텀 페이지 온보딩 (SDK_FULL) — 에이전트 실행 runbook

> 이 문서는 **AI 코딩 에이전트(Claude Code 등)가 읽고 그대로 수행**하는 실행 지시서다.
> 사람용 설명문이 아니라 상태 기계다. 각 단계를 **감지 → 검증 → 실행 → 재검증** 순으로 처리한다.
> 자동으로 할 수 있는 단계는 **멈추지 말고 계속 이어서** 다음 단계로 넘어간다. 사람 손이 꼭 필요한 지점에서만 잠깐 멈춘다.
>
> ⚠️ **이 문서를 요약해서 읽지 마라.** 명령·코드블록이 그대로 있어야 수행할 수 있다. 요약 프록시(일부 WebFetch)가 코드블록을 접었다면 raw 원문을 다시 확보하라:
> `curl -s https://raw.githubusercontent.com/cheese-bmf/onboarding-test/main/ONBOARDING.md`

## 진행 방식 — 체크포인트 & 재개 (중요)

이 온보딩은 **한 번에 끝내는 게 아니라, 사람 관문마다 멈췄다 이어가는 대화형 루프**다. "온보딩을 중단했습니다"로 세션을 끝내지 마라. 아래 규칙을 지켜라.

- **자동 구간은 쭉 진행한다.** 사람 개입이 필요 없는 검증·실행·재검증은 멈추지 말고 다음 단계까지 이어서 처리한다.
- **CHECKPOINT(사람 관문/실패)를 만나면 종료가 아니라 "일시정지"다.** 다음을 사용자에게 주고 **사용자의 재개 신호를 기다린다**:
  1. 지금 상태(어느 체크포인트에서 멈췄는지) — 원문 오류가 있으면 그대로.
  2. **사용자가 지금 해야 할 일** — 무엇을 · 어디서 · 왜, 구체적 조작까지.
  3. 재개 방법: **"처리하고 '완료했어'(또는 '됐어' / 'next')라고 말해주세요. 그러면 다시 검증하고 다음 단계로 이어갈게요."**
- **재개 신호("완료했어" 등)를 받으면**: 그 체크포인트의 **재검증 명령을 다시 실행**한다.
  - 통과하면 → 바로 다음 단계로 이어서 진행(또 자동 구간이면 쭉).
  - 여전히 실패하면 → 새 오류를 그대로 보고하고 **같은 체크포인트에서 다시 대기**(사용자가 고칠 때까지 이 루프를 반복).
- **추측·조작 금지**: 사용자가 처리했다고 한 걸 실제 검증 없이 "됐다 치고" 넘어가지 마라. 항상 재검증 명령으로 확인한 뒤 넘어간다.
- 문서에 없는 명령이 필요하면 지어내지 말고 `--help`로 확인하거나 사용자에게 물어라(이때도 종료가 아니라 대기).

## 이 온보딩이 만드는 것

b.stage SaaS의 특정 URL을 **커스텀 풀페이지(SDK_FULL)** 로 갈아 끼우는 3rd-party 템플릿 프로젝트. React 컴포넌트를 Web Component(IIFE 번들)로 빌드해 b.stage에 배포한다.

이 runbook의 도달 지점을 정직하게 밝힌다:
- **로컬까지(Phase 0~1)**: b.stage **계정·스테이지·조직·콘솔은 필요 없다.** 단 하나, SDK 설치용 **`PARTNERS_TOKEN`(사람이 발급하는 GitHub PAT)** 은 필요하다 — 이게 Phase 1의 유일한 사람 관문(G1)이고, 토큰만 있으면 스캐폴드·빌드·로컬 렌더까지 자동으로 간다.
- **배포까지(Phase 2)**: b.stage API 키·조직 접근·Console 활성화가 필요하며, 각각 명시적 WAITING으로 멈춘다.

## 단계 표기 규약

각 단계는 다음을 판정한다.

- `status`: `ok`(통과, 다음으로) | `blocked`(사람 관문/실패 → **CHECKPOINT 일시정지**, 재개 신호 대기) 
- `next_action`: 다음에 할 일 (blocked면 사용자가 할 일 + 재개 신호)
- 아래 **⛔ WAITING**으로 표시된 관문이 CHECKPOINT다. 종료가 아니라 위 "진행 방식"의 일시정지-재개 루프로 처리한다.
- 반쯤 된 상태로 다음 단계를 자동 진행하지 않는다(재검증 통과가 전제).

핵심 설계 원칙(따를 것):
1. **생성 먼저, 연결은 나중에.** 로컬 스캐폴드·빌드를 먼저 성공시키고, b.stage 관문(토큰·조직·콘솔)은 뒤로 몬다.
2. **명령을 지어내지 않는다.** 이 문서에 없는 플래그·명령이 필요하면 `--help`로 실제 CLI를 확인하고, 확인 안 되면 멈춰서 사용자에게 묻는다.
3. **성공은 "명령이 끝났다"가 아니라 "검증이 통과했다"로 정의한다.**

---

## Phase 0 — doctor (환경 진단 · 인증 불필요)

사람 개입 없이 항상 실행 가능. 로컬 문제와 b.stage 계정 문제를 분리하기 위해 먼저 돌린다.

**검증**
```bash
node -v      # v20 이상이어야 함
npm -v       # (또는 pnpm -v)
git --version
```

- Node가 20 미만 → CHECKPOINT. "Node 20+ 설치하고 '완료했어'라고 해주세요"라고 안내하고 대기 → 재개 신호 받으면 `node -v` 다시 확인 후 진행.
- 위가 모두 되면 `status: ok` → 멈추지 말고 Phase 1로 바로 이어간다.

**사전 준비물 체크 (Phase 1 진입 전):** Phase 1은 계정·스테이지 없이 되지만 **`PARTNERS_TOKEN` 하나는 반드시 필요**하다. 지금 없으면 아래 G1이 곧 WAITING이 되니, 먼저 발급 요청을 걸어두는 걸 권한다.

---

## Phase 1 — init (로컬 스캐폴드 + 로컬 렌더까지)

목표: b.stage **계정·스테이지 없이**(단, 아래 G1의 `PARTNERS_TOKEN`은 필요) 프로젝트를 만들고 로컬 개발 서버에 첫 화면을 띄운다.

### ⛔ WAITING G1 — PARTNERS_TOKEN (classic PAT)

SDK 패키지(`@partners-bmf/*`)는 npmjs가 아니라 **GitHub Packages(`https://npm.pkg.github.com`) 사설 레지스트리**에 있다. 그래서 **CLI를 가져오는 `npx @partners-bmf/bstage-cli` 자체가 인증·레지스트리 설정 전에는 실패**한다(npm이 기본 레지스트리 npmjs에서 찾다 404/401). 이 토큰은 **사람이 발급받아야 한다(자동화 불가).**

- **무엇을**: `PARTNERS_TOKEN` = GitHub **classic** PAT, scope 최소 `read:packages`
  - ⚠️ **Fine-grained PAT는 GitHub Packages를 아직 지원 안 함 → 반드시 classic 토큰**
  - 생성 후 토큰 목록에서 **Configure SSO → `partners-bmf` 조직 Authorize**까지 눌러야 함. 안 하면 private 패키지가 404/401
- **어디서**: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic). 발급 경로 문의는 b.stage 파트너 담당자
- **왜**: 없으면 CLI를 받는 `npx`도, 프로젝트 의존성 설치도 GitHub Packages 401/404로 실패
- **재개 방법(사용자에게 이렇게 안내)**: "토큰을 받아 `export PARTNERS_TOKEN=ghp_xxxx` 하고 **'완료했어'라고 말해주세요.** 그러면 검증하고 다음 단계로 이어갈게요."

> 존재 여부만 안전하게 확인한다(값을 출력하거나 로그에 남기지 말 것):
> ```bash
> [ -n "$PARTNERS_TOKEN" ] && echo "PARTNERS_TOKEN: set" || echo "PARTNERS_TOKEN: missing"
> ```
> `missing`이면 CHECKPOINT(위 진행 방식대로 안내 후 재개 신호 대기). `set`이면 아래 "레지스트리 설정"으로 이어간다.
> (이 관문 + 아래 레지스트리 수동 설정은 TO-BE 개선 대상이다 — TOBE.md G1 참조.)

### 실행 — 레지스트리 설정 (`.npmrc`) · `npx` 전에 반드시

`@partners-bmf` 스코프를 GitHub Packages로 매핑해야 CLI를 가져올 수 있다. **프로젝트를 만들 상위 디렉터리(지금 `npx`를 실행할 곳)에 `.npmrc`를 먼저 만든다.**

```bash
cat > .npmrc <<'NPMRC'
@partners-bmf:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${PARTNERS_TOKEN}
NPMRC
```
- `${PARTNERS_TOKEN}`은 npm이 실행 시 환경변수에서 읽는다. **토큰 원문을 `.npmrc`에 직접 쓰지 말 것.**
- (예전 예시에 있던 `always-auth=true`는 최신 npm에서 `Unknown project config` 경고를 낸다 — 넣지 않는다. 스코프 `_authToken` 한 줄로 인증은 충분하다.)
- 어드민 계보(`@frontend-bmf`)까지 쓸 거면 같은 방식으로 `@frontend-bmf:registry=...` 한 줄을 추가. SDK_FULL 유저 템플릿만이면 위 세 줄로 충분.
- **검증(재개 지점)**: `npm view @partners-bmf/bstage-cli version`
  - 버전이 출력되면 통과 → 바로 "프로젝트 생성"으로 이어간다.
  - **`E403 / 403 Forbidden ... does not match expected scopes`** → 토큰은 있지만 **scope나 SSO가 안 맞는 것**이다. CHECKPOINT로 이렇게 안내하고 대기:
    > "토큰이 있는데 GitHub Packages 권한이 안 맞아요(E403). classic PAT에 **`read:packages` scope**가 있는지, 그리고 토큰 목록에서 **`partners-bmf` 조직에 Configure SSO → Authorize** 했는지 확인해 주세요. (fine-grained면 classic으로 새로 발급.) 새 토큰이면 `export PARTNERS_TOKEN=ghp_xxxx` 까지 하고 **'완료했어'라고 말해주세요.** 다시 검증할게요."
  - **`401 / 404`** → 토큰 미인식/미설정. 위 G1로 돌아가 같은 방식으로 안내·대기.
  - 재개 신호를 받으면 이 `npm view`를 **다시 실행**해서 통과할 때까지 이 체크포인트를 반복한다(종료하지 않는다).

### 실행 — 프로젝트 생성

**네이밍 규칙 먼저(어기면 init이 바로 중단된다 — 추측 금지):**
- `--space`: 영문자로 시작, 영문·숫자·하이픈만. (예: `demo`)
- `--template`: **Custom Element 이름 규칙 — 소문자 시작 + 하이픈 최소 1개 + 소문자·숫자·하이픈만.** (예: `demo-hello`)
  - ⚠️ `hello`처럼 **하이픈 없는 값을 주면 `■ --template 값은 Custom Element 스펙...`으로 중단된다.** 반드시 하이픈을 넣어라(관례: `<space>-hello`).
  - `--template`을 **생략하면 기본값 `<space>-hello`** 가 자동으로 쓰이고 이건 항상 유효하다.

```bash
# 안전한 완전 지정형 (권장): 값이 모두 규칙을 만족
npx @partners-bmf/bstage-cli@latest init --yes --space demo --template demo-hello --target user --phase dev --pm npm
```
- **재개/부분 실패 처리:** 이 명령을 재실행하기 전에 `demo-custom-templates-dev`가 이미 있는지 확인한다. 있으면 init을 다시 실행하지 말고, init 출력에 표시된 실제 프로젝트 디렉터리로 들어가 생성 결과를 점검한 뒤 아래 검증 단계부터 재개한다. CLI는 기존 디렉터리가 있으면 중단하므로, 폴더가 있다는 이유만으로 삭제하거나 덮어쓰지 않는다.
  - 최소 점검: `package.json`과 `src/templates/<template>/template.tsx`가 있는지 확인한다.
  - 둘 다 있으면 `npm run dev` 검증으로 이어간다.
  - 하나라도 없거나 init이 원문 오류와 함께 중단됐다면 그 오류와 디렉터리 상태를 보고하고 CHECKPOINT에서 대기한다. 사용자가 정리·복구한 뒤 “완료했어”라고 하면 같은 점검을 다시 실행한다.
- `--yes`는 비대화형(에이전트/비TTY 환경 필수). API 키는 안 물어보고 없이 시작(나중 connect 단계에서 설정).
- 기본값: `--target user` `--phase dev` `--pm npm`(생략 가능). `--space`만 필수.
- 완료되면 프로젝트 폴더 **`<space>-custom-templates-<phase>`** (위 예시면 `demo-custom-templates-dev`)가 생기고, 그 안에 의존성 설치·git 초기화·보일러플레이트 + **첫 템플릿 `src/templates/<template>/template.tsx` 가 자동 생성**된다.
  > 폴더명이 다르게 보이면 init 출력에 찍힌 이름을 쓰거나 `ls -d */`로 새로 생긴 디렉터리를 찾아 그리로 `cd` 한다.

### (선택) 템플릿 수정·추가
init이 **첫 템플릿 `src/templates/<template>/template.tsx` 를 이미 생성**했다. happy path에선 손으로 만들 필요 없다. 내용만 고치면 된다.
새 템플릿을 **추가**할 때만 폴더를 만든다 — **폴더명 = 템플릿명은 Custom Element 규칙(소문자·하이픈 최소 1개, 예 `demo-profile`)**, 파일명은 반드시 `template.tsx`:
```tsx
import { createTemplate } from '@partners-bmf/bstage-react'

export default function ProfileTemplate() {
  return <div style={{ padding: 24 }}><h1>Hello, b.stage!</h1></div>
}

createTemplate(ProfileTemplate, {
  space: 'demo',
  name: 'demo-profile',   // 폴더명과 일치, 하이픈 규칙 준수
  // target 미지정 → 기본 'user', slot 미지정 → 풀페이지(SDK_FULL)
})
```

### 검증 — 로컬 성공 신호
```bash
cd demo-custom-templates-dev   # = <space>-custom-templates-<phase> (init이 만든 실제 폴더명)
npm run dev
```
- `http://localhost:5173` → 템플릿 목록, `http://localhost:5173/<template>` (예: `/demo-hello`) → 컴포넌트 렌더 확인.
- 여기까지 뜨면 **Phase 1 `status: ok`** (b.stage 계정 없이 도달하는 첫 성공 지점) → 다음(Phase 2)로 이어가되, 대상 스테이지가 없으면 거기 관문들은 CHECKPOINT로 안내.

### Phase 1 자가진단
**init이 중단될 때 (증상 → 원인):**
- `■ --template 값은 Custom Element 스펙...` → `--template`(또는 템플릿명)에 **하이픈이 없다.** `hello`❌ → `demo-hello`✅. 아예 생략하면 기본 `<space>-hello`로 통과.
- `--space 값은 영문자로 시작...` → space가 규칙 위반(영문 시작·영숫자·하이픈만).
- `PARTNERS_TOKEN 환경변수가 설정되지 않았습니다` → G1로.
- `403 ... does not match expected scopes` → 토큰 scope/SSO 문제(레지스트리 검증 CHECKPOINT).

**렌더가 안 되거나 빌드가 깨질 때:**
- [ ] `createTemplate()` 이 **모듈 최상위**에서 호출되는가 (함수 내부 호출 금지 — 빌드 시 메타데이터 추출 실패)
- [ ] 컴포넌트를 **`export default`** 로 내보냈는가
- [ ] 파일 경로가 정확히 `src/templates/<template>/template.tsx` 인가 (`<template>`은 하이픈 포함 이름)
- [ ] CSS는 `?inline` 로 import해서 `<style>` 로 주입했는가 (Shadow DOM이라 외부 CSS 미적용)
- [ ] `.npmrc`·`PARTNERS_TOKEN` 설정이 맞는가 (설치 401/403이면 G1 재확인)

---

## Phase 2 — connect (b.stage 관문 · 배포)

여기서부터는 **b.stage 계정·대상 스테이지·조직·콘솔**이 필요하다. Phase 1의 로컬 렌더가 성공했더라도 배포 대상이 없으면 배포 완료로 판정하지 않는다. Phase 2는 아래 상태를 **한 번에 하나씩** 처리한다.

```text
WAITING_STAGE → G2 → G3 → G4 → DEPLOY_VERIFY → 완료
```

현재 상태에서 사용자에게는 이번 단계의 작업 하나만 안내한다. 사용자가 **“완료했어”**라고 말하면 에이전트는 현재 상태의 재검증만 실행하고, 통과한 경우에만 다음 상태로 이동한다. 실패하면 원문 오류와 보완 작업을 안내한 뒤 같은 상태에서 다시 대기한다.

### 브라우저 보조와 접근 제한

- 브라우저 보조가 가능한 환경에서는 `browser:control-in-app-browser` 스킬로 현재 단계에 필요한 화면이나 URL을 열고, 사용자가 눌러야 할 버튼·입력할 값을 단계별로 안내한다.
- 로그인·인증이 필요한 화면에서는 비밀값을 대신 입력하거나 출력하지 않는다. 사용자가 직접 입력한 뒤 “완료했어”라고 알려야 한다.
- b.stage Console처럼 외부 에이전트가 접근할 수 없는 내부 화면은 조작하거나 우회하지 않는다.
- 브라우저를 열 수 없거나 화면 접근이 거부되면, 확인된 URL·화면명·클릭/입력 순서와 대체 수동 절차를 안내한다. 확인되지 않은 URL이나 명령은 추측하지 않는다.

### ⛔ WAITING_STAGE — 배포 대상 스테이지 없음

- **현재 상태**: 로컬 프로젝트와 렌더는 완료됐지만, 연결·배포할 대상 b.stage 스테이지가 아직 없다. 이는 실패가 아니라 Phase 2를 시작할 수 없는 정상적인 대기 상태다.
- **이번에 사용자가 할 일 하나**: b.stage에서 사용할 대상 스테이지를 만들거나, 담당자에게 스테이지 ID/URL을 요청한다. G2~G4 준비는 이 단계가 통과한 뒤 안내한다.
- **어디서 확인하는지**: 브라우저 보조가 가능하면 b.stage 스테이지 관리 화면을 열어 사용자가 생성·선택할 위치를 안내한다. 브라우저를 열 수 없으면 확인된 스테이지 관리 URL과 화면명을 제시하고, URL을 확인할 수 없으면 담당자에게 링크를 요청하는 수동 절차로 전환한다.
- **왜 필요한지**: 스테이지가 없으면 G2 API 인증의 대상, G4 활성화할 URL, 배포 후 실제 화면을 확인할 위치가 모두 정해지지 않는다.
- **완료 판정**: 사용할 스테이지 ID/URL이 정해지고, 사용자가 해당 스테이지에 접근할 수 있어야 한다. G2~G4를 미리 완료할 필요는 없다.
- **재개 흐름**: 사용자가 준비를 마치고 **“완료했어”**라고 말하면 에이전트는 스테이지 ID/URL의 접근 가능 여부만 다시 확인한다. 통과하면 G2에서 다음 한 작업을 안내하고, 여전히 대상이 없거나 접근할 수 없으면 원문 오류와 수동 조치를 안내한 뒤 `WAITING_STAGE`에서 다시 대기한다.

### ⛔ WAITING G2 — API 자격증명 (appId / appSecret)
- **현재 상태**: API를 호출하는 템플릿인데 `appId`/`appSecret`이 없거나, 설정됐지만 실제 대상 스테이지에 인증되는지 확인되지 않았다. API 호출이 필요 없는 순수 화면이면 이 관문은 `not_required`로 기록하고 건너뛸 수 있다.
- **무엇을**: `appId`(`bsa_...`), `appSecret`(`bsp_...`)
- **어디서**: 파트너 콘솔에서 발급
- **왜**: `src/shared/client.ts`의 `BstageClient` 인증에 사용
- **이번에 사용자가 할 일 하나**: 파트너 콘솔에서 `appId`와 `appSecret`을 발급받아 프로젝트의 `src/shared/client.ts`에 설정한다. `appId`는 `bsa_...`, `appSecret`은 `bsp_...` 형식이어야 하며, 토큰·Secret 원문은 대화나 로그에 붙여 넣지 않는다.
- **어디서 확인하는지**: 브라우저 보조가 가능하면 파트너 콘솔의 API 자격증명 발급 화면을 열어 발급 버튼과 설정 위치를 안내한다. 브라우저를 열 수 없거나 인증 화면에 접근할 수 없으면 확인된 콘솔 URL·화면명·발급→설정 순서를 안내하고 사용자가 직접 처리한다. 발급 상태는 파트너 콘솔에서, 설정 위치와 형식은 프로젝트의 `src/shared/client.ts`에서 확인한다.
- **왜 필요한지**: `BstageClient`가 대상 스테이지의 인증 API를 호출할 때 이 두 값이 필요하다. 없거나 뒤바뀌면 화면은 떠도 API 호출이 인증 에러로 실패한다.
- **완료 판정**: API를 쓰는 템플릿은 두 값이 올바른 위치에 설정되고, 비밀값을 노출하지 않은 상태에서 대상 스테이지에 대한 인증 API 검증이 성공해야 한다. 순수 화면 템플릿은 API 호출이 없다는 근거를 확인한 뒤 `not_required`로 판정한다.
- **재개 흐름**: 사용자가 이 작업을 마치고 **“완료했어”**라고 말하면 에이전트는 `client.ts`의 키 존재 여부·접두사·역할을 비밀값 없이 점검하고, 문서나 SDK에 정의된 실제 인증 확인 경로가 있으면 그것을 실행한다. 검증이 통과하면 G3에서 다음 한 작업을 안내하고, 실패하면 원문 인증 오류와 수정할 위치를 안내한 뒤 G2에서 다시 대기한다.

### ⛔ WAITING G3 — GitHub 조직 접근 (배포 리포)
- **현재 상태**: 배포 리포가 사설 GitHub 조직에 있거나, 조직 멤버십이 아직 승인되지 않았다. 승인 전에는 배포용 리포를 찾거나 push하려고 시도하지 않는다.
- **이번에 사용자가 할 일 하나**: flex 기안으로 배포 조직(`partners-bmf` 등)의 접근 권한을 신청하고, 승인 후 포털에 표시된 배포 리포 URL을 확인한다. 승인 전이라면 이 신청만 진행한다.
- **어디서 확인하는지**: 브라우저 보조가 가능하면 GitHub 조직과 포털이 안내한 배포 리포 화면을 열어 멤버십·리포 접근 위치를 안내한다. 브라우저를 열 수 없거나 로그인 세션이 없으면 정확한 조직명·리포 URL·화면명을 기준으로 flex 신청→승인 확인→리포 열기 순서를 수동 안내한다. 리포 링크가 404이면 멤버십 또는 리포 URL이 아직 해결되지 않은 것이다.
- **왜 필요한지**: 배포는 이 조직의 리포에 push하면 CI가 빌드·업로드하는 구조라서, 접근 권한이 없으면 배포 자체를 시작할 수 없다.
- **완료 판정**: 승인된 계정으로 배포 리포를 열 수 있고, 포털이 안내한 정확한 리포 URL에 대해 읽기/푸시 권한을 확인할 수 있어야 한다. 단순히 flex 신청서를 제출한 것만으로는 통과시키지 않는다.
- **재개 흐름**: 사용자가 이 작업을 마치고 **“완료했어”**라고 말하면 에이전트는 포털에 표시된 리포 URL을 기준으로 GitHub 접근만 재검증한다. 브라우저 보조가 가능하면 리포 화면을 다시 열고, 불가능하면 확인된 URL에 대해 사용자가 제공한 접근 결과를 검증한다. 리포 접근이 확인되면 G4에서 다음 한 작업을 안내하고, 404/권한 오류가 계속되면 원문 오류와 필요한 권한을 안내한 뒤 G3에서 다시 대기한다.

### ⛔ WAITING G4 — b.stage Console 커스텀 템플릿 활성화
- **현재 상태**: 배포 리포 접근은 가능하지만, b.stage Console에서 해당 커스텀 템플릿/URL이 활성화됐는지 확인되지 않았다. Console은 bemyfriends 내부 제품이므로 외부 에이전트가 직접 활성화할 수 없다.
- **이번에 사용자가 할 일 하나**: 내부 담당자에게 대상 스테이지, 커스텀 템플릿명/URL, 배포 환경을 전달하고 Console에서 해당 템플릿/URL을 **활성화 ON**으로 요청한다. 활성화 전에는 배포 성공 여부만으로 완료 처리하지 않는다.
- **어디서 확인하는지**: 브라우저 보조가 가능하고 Console 접근 권한이 있으면 활성화 화면을 열어 해당 템플릿/URL의 ON 상태를 안내한다. 내부 인증이나 Console 접근이 불가능하면 Console URL·화면명·활성화 ON 요청 단계와 담당자에게 전달할 값을 수동 안내하고, 실제 상태 확인은 담당자에게 요청한다. 대상 스테이지의 커스텀 URL은 활성화 후 별도로 확인한다.
- **왜 필요한지**: 리포 업로드와 Console 활성화는 별개다. 활성화하지 않으면 배포가 성공해도 라우팅이 적용되지 않아 접속 시 404가 난다.
- **완료 판정**: Console에서 해당 템플릿/URL이 활성화 상태이고, 배포 후 대상 URL에서 커스텀 화면이 실제로 표시되어야 한다. 업로드 성공 체크마크만 있거나 URL이 404이면 G4는 미해결이다.
- **재개 흐름**: 사용자가 담당자 확인 후 **“완료했어”**라고 말하면 에이전트는 Console의 활성화 상태만 다시 확인한다. 브라우저 보조가 가능하면 활성화 화면을 재확인하고, 불가능하면 담당자의 확인 결과를 근거로 판정하되 미확인 상태를 성공으로 추측하지 않는다. 활성화가 확인되면 `DEPLOY_VERIFY`에서 다음 한 작업을 안내하고, 미활성·권한 오류가 계속되면 원인과 다음 조치를 안내한 뒤 G4에서 다시 대기한다.
- 이 관문은 외부 자동화가 원천 불가하므로, 활성화 여부를 확인할 수 없는 경우에도 성공으로 추측하지 않는다(TOBE.md G4).

### ⏳ DEPLOY_VERIFY — 배포 결과 확인

- **현재 상태**: G2~G4는 통과했지만, 업로드 성공과 실제 라우팅·릴리즈 반영은 아직 확인되지 않았다.
- **이번에 사용자가 할 일 하나**: 안내된 배포 절차가 끝난 뒤 대상 스테이지의 커스텀 URL을 연다. 업로드 성공 체크마크만으로 완료 처리하지 않는다.
- **어디서 확인하는지**: 브라우저 보조가 가능하면 대상 URL을 열어 HTTP 오류, 404 여부, 커스텀 화면 렌더를 확인한다. 브라우저를 열 수 없으면 정확한 대상 URL과 화면명, 새로고침·릴리즈 반영 확인 순서를 안내한다.
- **왜 필요한지**: 업로드와 릴리즈·라우팅 적용은 별개라서 배포가 성공해도 URL이 404이거나 기존 화면이 보일 수 있다.
- **완료 판정**: 대상 URL에서 404가 없고 커스텀 화면이 실제로 표시되며, 자가진단 표에 미해결 항목이 없어야 한다.
- **재개 흐름**: 사용자가 URL 확인 후 **“완료했어”**라고 말하면 에이전트는 대상 URL을 다시 열어 응답과 화면을 확인한다. 정상 렌더면 Phase 2를 완료하고, 404·기존 화면·릴리즈 미적용이면 원문 상태를 보고하고 필요한 담당자 조치를 안내한 뒤 `DEPLOY_VERIFY`에서 다시 대기한다.

### 실행 — 배포 (권한이 갖춰진 경우)
빌드·업로드는 CI가 자동 처리한다. 개발자가 직접 빌드할 필요 없음.
```bash
# main push → dev, qa 환경 자동 배포
git push origin main

# real 환경 배포 → 태그 push
git tag v1.0.0 && git push origin v1.0.0
```
수동으로 산출물만 확인하려면 `npx bstage build` → `dist/<target>/<space>/<name>/{template.js, manifest.json}` 생성.

### Phase 2 배포 자가진단 (증상 → 원인)
| 증상 | 원인 / 확인 |
|---|---|
| 배포는 성공인데 접속하면 전부 404 | **Console 활성화 안 켬** (G4). 가장 흔함 |
| 성공 체크마크인데 화면이 안 바뀜 | **업로드 ≠ 릴리즈**. 업로드만 하고 릴리즈(라우팅 적용)를 안 걸었다 (별개 단계) |
| 업로드(빌드) 실패 | SDK 매핑 경로가 `manifest.json`이 실제로 놓이는 디렉토리명과 안 맞음 |
| 재배포 버튼이 비활성 | 노출 중이라서. 미노출로 내려야 열림. (미노출 저장이 반영 안 되는 버그 존재 — 막히면 담당자에게) |
| API 호출이 인증 에러 | `bsa_`/`bsp_`를 뒤바꿔 넣었거나 `client` 연결 한 줄 누락 (G2) |
| 레포 링크 눌렀는데 GitHub 404 | 조직 접근 권한 없음 → flex 기안 (G3) |

---

## 완료 정의

- **로컬 완료(계정·스테이지 없이, `PARTNERS_TOKEN`만 있으면 도달)**: Phase 0 ok + Phase 1 ok (localhost:5173에 컴포넌트 렌더).
- **배포 완료(관문 통과 필요)**: WAITING_STAGE→G2→G3→G4→DEPLOY_VERIFY를 순서대로 통과하고, 배포 접속 시 커스텀 화면 정상 표시 + 자가진단 표에 걸린 항목 없음.

대상 스테이지가 아직 없으면 **로컬 완료까지가 이번 온보딩의 성공 지점**이고, Phase 2는 `WAITING_STAGE`에서 스테이지 준비 한 가지만 안내한 뒤 사용자의 “완료했어” 신호를 기다리는 것이 올바른 종료다.

## 이 runbook에서 만난 WAITING을 기록하자

각 WAITING(G1~G4)에서 실제로 어디서·왜 막혔는지 한 줄씩 남기면, 그게 포털이 없앨 마찰의 근거가 된다. 개선 방향은 [TOBE.md](./TOBE.md)에 정리돼 있다.
